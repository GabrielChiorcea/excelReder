import style from './title.module.scss';

const Title = () => {
  return (
    <div className={style.titleContainer}>
      <h2 className={style.title}>Powers</h2>
    </div>
  );
};

export default Title;
